export interface EditUserFormValues {
  name: string;
  email: string;
  medicalStoreId: string;
}

export interface NormalizedEditUserValues {
  name: string;
  email: string;
  medicalStoreId: string;
}
